name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    steps:
      - name: Deploy
        env:
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
        run: |
          npm install
          npm run build
          gh-pages -d build
